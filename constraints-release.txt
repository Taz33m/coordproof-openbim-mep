# Exact direct package versions used for the committed golden evidence.
# CI and release regeneration install this file with requirements.txt so the
# CAD/IFC serialization toolchain is deliberate and reproducible. Supported
# version ranges remain declared in requirements.txt for separate compatibility
# testing. The key runtime versions are recorded in build provenance.
cadquery==2.5.2
cadquery-ocp==7.9.3.1.1
defusedxml==0.7.1
ifcopenshell==0.8.5
ezdxf==1.4.4
reportlab==5.0.0
Pillow==12.3.0
jsonschema==4.26.0
pytest==9.1.1
